<div class="nested-fields">
  <%= f.collection_select :item_id, Item.all, :id, :name, prompt: 'Seleziona articolo', class: 'form-control' %>
  <%= f.text_field :quantity %>
  <%= link_to_remove_association "Cancella", f, class:"btn btn-danger" %>
</div>
<br>
