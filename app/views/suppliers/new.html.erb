<!-- app/views/orders/new.html.erb -->
<div class="container">
  <div class="d-flex">
    <h2>Crea nuova Anagrafica</h2>
  </div>

<%= form_with(model: @supplier, local: true) do |form| %>
  <!-- User form fields -->
    <div>
  <%= form.label :name %>
      <br>
  <%= form.text_field :name %>
      <%= @supplier.errors[:name].first if @supplier.errors[:name].any? %>
    </div>

      <div>
  <%= form.label :Cognome %>
        <br>
  <%= form.text_field :surname %>
        <%= @supplier.errors[:surname].first if @supplier.errors[:surname].any? %>
        </div>

          <div>
  <%= form.label :Email %>
            <br>
  <%= form.text_field :email %>
            <%= @supplier.errors[:email].first if @supplier.errors[:email].any? %>
          </div>

    <br>
    <div class="inline">
      <td><%= link_to 'Annulla', suppliers_path, class: "btn btn-light" %></td>
      <td><%= form.submit 'Conferma', class: 'btn btn-primary'%></td>
  </div>
<% end %>
</div>
